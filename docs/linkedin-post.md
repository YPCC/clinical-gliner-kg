# LinkedIn post (ready to paste)

Follow the 4-part architecture. **Do not put the GitHub URL in the first publish.** Wait two minutes, then edit.

## Draft (no URL)

```
Stop sending every clinical note to an LLM.

GLiNER already finds entities and relations.

oaklib grounds them on local files.

No BioPortal key is required.

You keep Mondo or ChEBI on disk.

spaCy-LLM only sees the hard cases.

The graph stores Cypher with provenance.

📌 Zero-shot GLiNER 2.5 extraction
🚀 Local oaklib, not a remote API
💡 LLM as exception handler, not default

Where does your extraction plane still need a foundation model?

👇 [Repo + oaklib docs — ADD THIS ONLY DURING STEP 4 EDIT]
[LINK_HERE - ADD THIS ONLY DURING STEP 4 EDIT]
```

## Publishing workflow

1. **Draft** — paste the block above. No URLs.
2. **Publish immediately.**
3. **Wait exactly 2 minutes** (dwell time before the edit).
4. **Edit the post** and replace the placeholder with:

```
👇 Code, mermaid cascade, and oaklib local-file notes
https://github.com/YPCC/clinical-gliner-kg
oaklib docs: https://incatools.github.io/ontology-access-kit/
```

Attach `docs/images/cascade-architecture.jpg` as the post image (optional second image: `cost-planning-matrix.jpg`).

## Sentence check

Every body sentence is under 10 words. Hook is 9 words. Three emoji bullets only. CTA is a comment question, not a “link in comments” ask.
